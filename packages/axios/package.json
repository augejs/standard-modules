{
  "name": "@augejs/axios",
  "version": "1.0.1-rc.2",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc --build ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex.Zhang",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "axios": "^0.20.0"
  },
  "files": [
    "./dist/**"
  ],
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
