{
  "name": "@augejs/sequelize",
  "version": "1.0.1-rc.1",
  "description": "",
  "main": "./dist/main.js",
  "source": "src/main.ts",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc --build ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex.Zhang",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "sequelize": "^6.3.5"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
